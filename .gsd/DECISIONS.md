# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001/S02 | arch | Tool slot concurrency model | Per-task-id tracking instead of per-agent-name max-1 slot | Current acquire_tool_slot enforces max 1 per agent name globally, contradicting parallel execution. Multiple Claude workers need to run simultaneously. Track by task_id with optional per-agent-type concurrency limit | Yes — if rate limits make unbounded parallelism impractical | agent |
| D002 | M001/S02 | arch | Worker execution directory | Each worker runs in its own git worktree via WorktreeManager.create_for_task() | WorktreeManager exists with full API and tests. Wire into orchestrator dispatch so each worker's cwd is the worktree path. Core safety promise for parallel execution | No | agent |
| D003 | M001/S03 | pattern | Task completion event matching | Use dag_id from backend event payload + dagToFrontendId map, not FIFO queue | Backend already sends dag_id in task_completed/task_failed events. Frontend ignores it and uses subTaskQueue.shift(). dagToFrontendId map is maintained — just needs to be used | No | agent |
| D004 | M001/S03 | pattern | activePlan availability timing | Set activePlan from @@orch:: events during Phase 1, not after promise resolves | Promise resolves after ALL phases. activePlan needed during awaiting_approval. Plan info available from @@orch:: events at Phase 1 start | No | agent |
| D005 | M002 | arch | M001 code recovery vs fresh start | Fresh start on gsd-main — M002 re-implements all M001 scope from actual codebase state | M001 worktree was cleaned up without squash-merging code changes. Planning artifacts say "complete" but codebase is pre-M001 state. Recovering stale worktree changes is risky and potentially conflicting. Cleaner to fix from what actually exists. | No | agent |
| D006 | M002/S01 | pattern | DAG node ID assignment strategy when LLM provides task IDs | All-or-nothing: if ALL tasks have LLM-provided id, use them; if ANY is missing, ALL fall back to index-based t1/t2/t3 | Partial LLM IDs create chaos — depends_on references from tasks with IDs can't resolve against index-generated IDs on tasks without. All-or-nothing keeps the DAG internally consistent. | Yes — if LLMs consistently provide IDs, the fallback path becomes dead code | agent |
| D007 | M002/S01 | pattern | decomposition_failed event semantics — informational vs terminal | Emit decomposition_failed on all 4 failure paths; informational-only on fallback path (emit then continue), terminal on Err paths (emit then return) | The fallback single-task path is a degraded success, not a terminal failure — the user should see a warning but orchestration continues. Process error, timeout, and auth error are terminal — the event precedes return Err. | Yes | agent |
| D008 |  | M002/S02 | Tool slot key strategy for parallel same-agent workers | Change tool slot key from agent name (tool_name) to unique dispatch_id (task UUID). Remove the running-process check that blocked concurrent same-agent dispatches. The per-dispatch reservation still prevents TOCTOU races on rapid dispatch of the same task_id. | The existing `acquire_tool_slot("claude")` design enforces a global max-1-process-per-agent-name constraint that fundamentally prevents R004 (parallel same-agent workers). The original TOCTOU protection goal is preserved by keying on dispatch_id instead — each dispatch still gets an atomic reservation, but two different dispatches (even for the same agent) don't conflict. The running-process check is unnecessary once reservations are per-dispatch. | No — this is a prerequisite for parallel execution and reverting would re-break R004 | agent |
| D009 |  | M002/S02 | Parallel dispatch strategy for DAG wave execution | Use tokio::task::JoinSet to spawn all workers in a wave concurrently, with an extracted dispatch_and_await_worker() async helper that encapsulates the per-worker dispatch+wait+retry+fallback logic. Create a dispatch_task_inner() non-Tauri-command variant for spawned task compatibility. | JoinSet is the standard Tokio pattern for spawning a bounded set of tasks and collecting results. Extracting the per-worker body preserves the retry/fallback logic without duplication. A dispatch_task_inner() variant is needed because tauri::State has a non-'static lifetime that prevents use in spawned tasks — the inner AppState (Arc) is Send+Sync and clone-friendly. | Yes — could switch to futures::join_all or manual select! if JoinSet semantics prove limiting | agent |
| D010 | M002/S02/T03 | architecture | How to call dispatch_task from spawned Tokio tasks where tauri::State lifetimes cannot be satisfied | Created dispatch_task_inner() in router.rs that accepts AppState and ContextStore directly, mirroring dispatch_task but calling spawn_with_env_core (made public) instead of per-agent spawn functions that require tauri::State wrappers | Spawned Tokio tasks (JoinSet workers) cannot carry tauri::State<'_> references. Rather than modifying the #[tauri::command] dispatch_task signature (which would break the Tauri command boundary), a parallel inner function preserves both the Tauri-facing API and enables spawned-task dispatch. Making spawn_with_env_core public avoids duplicating spawn logic. | Yes | agent |
| D011 | M002/S02/T02 | M002/S02 | Behavior when worktree creation fails for a worker task | Treat as dispatch-blocking error — add task to failed_dag_ids and skip. Do NOT fall back to running in project_dir. | Running a worker in project_dir when worktree creation fails would violate the core isolation promise (R003) and risk corrupting the main repo with parallel writes. Failing the individual task is safer — the user sees a clear error and other worktree-backed workers continue. The failed task can be retried manually. | No — falling back to project_dir would undermine the isolation guarantee | agent |
| D012 | M002/S03/T02 | pattern | useShallow scope — which Zustand selectors to wrap | Wrap multi-property object selectors with useShallow. Keep function selectors (setters), single-property selectors, and derived/computed selectors as individual calls. | Setters are stable Zustand references that never change — useShallow adds overhead with no benefit. Single-property selectors already use strict equality. Derived selectors (e.g., computing doneTasks from tasks Map) return new values on every call, defeating shallow comparison. Only multi-property object selectors benefit because they return new object references on every store mutation. | Yes | agent |
| D013 | M002/S04 | architecture | Worktree auto-commit and diff generation ordering within orchestration pipeline | Sequential per-worktree auto-commit and diff generation (Phase 2.5 block) between Phase 2 worker completion and Phase 3 review start. Not parallelized. | git2 operations on worktrees that share the same parent repository have contention on the git object store and index. Parallel auto-commits would risk corruption or lock errors. Sequential processing is safe and fast enough (each commit/diff is <100ms). The pattern also simplifies error handling — a failure on one worktree doesn't affect others. | Yes — if worktree count grows large, could batch with per-repo locking | agent |
| D014 | M002/S04 | architecture | Review prompt diff truncation strategy | Proportional truncation — each worktree gets an equal share of ~20KB total budget. Diffs exceeding their share are truncated with a note. | Review agent context windows have finite capacity. Fixed 20KB total prevents prompt bloat regardless of worktree count. Proportional allocation ensures no single large worktree crowds out smaller ones. The truncation note tells the review agent that context is incomplete. | Yes — could increase budget for models with larger context, or use summarization instead of truncation | agent |
