---
phase: 08-prompt-engine
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/prompt/PromptPreview.tsx
  - src/hooks/useTaskDispatch.ts
  - src/routes/index.tsx
  - src/components/terminal/ProcessPanel.tsx
autonomous: false
requirements: [PMPT-03]

must_haves:
  truths:
    - "User can click a Preview button before sending a task to see the optimized prompt for each tool"
    - "Preview panel shows side-by-side optimized prompts for Claude and Gemini"
    - "Preview fetches fresh context each time it opens (no stale cache)"
    - "User can close the preview and still send the task normally"
  artifacts:
    - path: "src/components/prompt/PromptPreview.tsx"
      provides: "Side-by-side prompt preview panel"
      min_lines: 30
    - path: "src/hooks/useTaskDispatch.ts"
      provides: "Updated dispatch hook (unchanged API, preview is separate)"
  key_links:
    - from: "src/components/prompt/PromptPreview.tsx"
      to: "optimizePrompt IPC"
      via: "commands.optimizePrompt call on open"
      pattern: "commands\\.optimizePrompt"
    - from: "src/components/terminal/ProcessPanel.tsx"
      to: "src/components/prompt/PromptPreview.tsx"
      via: "PromptPreview rendered in ProcessPanel with toggle state"
      pattern: "PromptPreview"
---

<objective>
Add a prompt preview UI that lets users see the optimized prompt for each tool before dispatching. Integrates into the existing ProcessPanel task submission flow.

Purpose: Users need transparency into how their prompt is transformed — trust requires seeing before sending (PMPT-03).
Output: PromptPreview component, preview toggle in ProcessPanel, human-verified end-to-end flow.
</objective>

<execution_context>
@/Users/birkankader/.claude/get-shit-done/workflows/execute-plan.md
@/Users/birkankader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prompt-engine/08-RESEARCH.md
@.planning/phases/08-prompt-engine/08-01-SUMMARY.md

@src/routes/index.tsx
@src/hooks/useTaskDispatch.ts
@src/components/terminal/ProcessPanel.tsx
@src/bindings.ts

<interfaces>
<!-- From Plan 01 output — OptimizedPrompt type available via bindings -->
<!-- After Plan 01, src/bindings.ts will contain: -->
```typescript
export type OptimizedPrompt = {
  tool_name: string;
  original_prompt: string;
  optimized_prompt: string;
};

// IPC command:
export function optimizePrompt(prompt: string, projectDir: string): Promise<Result<OptimizedPrompt[], string>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PromptPreview component and integrate into ProcessPanel</name>
  <files>src/components/prompt/PromptPreview.tsx, src/components/terminal/ProcessPanel.tsx</files>
  <action>
    **src/components/prompt/PromptPreview.tsx** (new file): Create a collapsible preview panel:

    Props: `{ prompt: string; projectDir: string; visible: boolean; onClose: () => void }`

    Behavior:
    - When `visible` becomes true AND `prompt.trim()` is non-empty, call `commands.optimizePrompt(prompt, projectDir)`
    - Show loading state while fetching ("Optimizing..." text)
    - On success, render side-by-side cards for each tool's optimized prompt
    - Each card shows: tool name header (e.g., "Claude", "Gemini"), optimized prompt in a `<pre>` block with `whitespace-pre-wrap`, scrollable with `max-h-48 overflow-y-auto`
    - On error or empty prompt, show a subtle "Enter a prompt to preview" message
    - Close button calls `onClose`
    - No caching — every time `visible` toggles to true, re-fetch (Pitfall 3 from research)
    - Styling: `border-t border-zinc-800 bg-zinc-900/80 p-3` container, `bg-zinc-800 rounded p-2` per tool card, `text-xs font-mono` for prompt text

    **src/components/terminal/ProcessPanel.tsx** — Add preview toggle:
    - Add `const [showPreview, setShowPreview] = useState(false)` state
    - Add a "Preview" button next to the existing submit/dispatch controls in the task input area
    - Button: `text-xs text-zinc-400 hover:text-zinc-200 border border-zinc-700 rounded px-2 py-1`
    - When clicked, toggle `showPreview`
    - Render `<PromptPreview prompt={currentPrompt} projectDir={projectDir} visible={showPreview} onClose={() => setShowPreview(false)} />` below the task input bar but above the terminal output
    - The preview panel should NOT block task submission — user can still send while preview is open

    Read ProcessPanel.tsx first to understand the current layout structure and where the task input lives. Place the Preview button adjacent to existing action buttons. Place PromptPreview component between the input bar and the terminal output area.
  </action>
  <verify>
    <automated>cd /Users/birkankader/Documents/Projects/WhaleCode && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>PromptPreview component renders side-by-side optimized prompts for Claude and Gemini. Preview button visible in ProcessPanel task input area. Preview fetches fresh data each time it opens. Existing vitest suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify prompt preview end-to-end flow</name>
  <action>Human verifies the complete prompt engine and preview flow works correctly in the running app.</action>
  <what-built>Complete prompt engine flow: user writes prompt, clicks Preview to see tool-specific optimized versions side by side, then dispatches. Optimized prompts include project context from ContextStore and use tool-specific formatting (Claude gets planning preamble, Gemini gets flat structure).</what-built>
  <how-to-verify>
    1. Run `cd /Users/birkankader/Documents/Projects/WhaleCode && cargo tauri dev`
    2. Enter a project directory path in the Project input
    3. Type a task prompt in the task input (e.g., "Refactor the auth module to use JWT tokens")
    4. Click the "Preview" button next to the submit controls
    5. Verify: Preview panel appears showing two side-by-side cards (Claude and Gemini)
    6. Verify: Claude card shows "## Task Plan" planning preamble, structured context section, and the task
    7. Verify: Gemini card shows flat "Context:" section followed by "Task:" — no planning preamble
    8. Verify: Both cards include project context if any context events exist in the store
    9. Click "Close" on the preview panel — verify it disappears
    10. Submit the task normally — verify it dispatches correctly (task appears in StatusPanel)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` — all frontend tests pass
- `cargo tauri dev` launches without errors
- Preview button visible in task input area
- Preview panel shows distinct Claude and Gemini optimized prompts
- Task dispatch still works after prompt engine integration
</verification>

<success_criteria>
- User can preview optimized prompts for each tool before sending (PMPT-03)
- Preview shows tool-specific formatting differences
- Preview refreshes on each open (no stale data)
- Normal task dispatch flow unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/08-prompt-engine/08-02-SUMMARY.md`
</output>
