---
phase: 10-ui-polish-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - src/routes/index.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/AppShell.tsx
  - src/components/WorktreeStatus.tsx
  - src/components/status/StatusPanel.tsx
  - src/components/terminal/ProcessPanel.tsx
  - src/components/settings/ApiKeySettings.tsx
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tooltip.tsx
  - src/index.css
autonomous: false
requirements: [POLISH-06]

must_haves:
  truths:
    - "Buttons across the app use shadcn Button component with consistent variants"
    - "WorktreeStatus panel uses shadcn Card for container styling"
    - "Project directory input uses shadcn Input component"
    - "Status badges use shadcn Badge component"
    - "Dark mode CSS variables from shadcn are active (dark class on html root)"
    - "Overall visual consistency improved across Sidebar, ProcessPanel, StatusPanel"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button with variant/size props"
      contains: "buttonVariants"
    - path: "src/components/ui/card.tsx"
      provides: "shadcn Card container component"
      contains: "CardHeader"
    - path: "src/components/ui/input.tsx"
      provides: "shadcn Input component"
      contains: "Input"
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge for status indicators"
      contains: "badgeVariants"
  key_links:
    - from: "src/routes/index.tsx"
      to: "src/components/ui/button.tsx"
      via: "import Button"
      pattern: "from.*@/components/ui/button"
    - from: "src/components/WorktreeStatus.tsx"
      to: "src/components/ui/card.tsx"
      via: "import Card"
      pattern: "from.*@/components/ui/card"
---

<objective>
Modernize the UI by replacing hand-styled elements with shadcn components for visual consistency across the app.

Purpose: Consistent, polished appearance using the design system already configured in the project. Focus on high-visibility surfaces: Sidebar, project bar, StatusPanel, WorktreeStatus, ProcessPanel tab area.
Output: All major UI surfaces use shadcn Button, Card, Input, Badge components. Dark mode CSS variables active.
</objective>

<execution_context>
@/Users/birkankader/.claude/get-shit-done/workflows/execute-plan.md
@/Users/birkankader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish-bug-fixes/10-RESEARCH.md
@.planning/phases/10-ui-polish-bug-fixes/10-02-SUMMARY.md

@src/routes/index.tsx
@src/components/layout/Sidebar.tsx
@src/components/layout/AppShell.tsx
@src/components/WorktreeStatus.tsx
@src/components/status/StatusPanel.tsx
@src/components/terminal/ProcessPanel.tsx
@src/components/settings/ApiKeySettings.tsx
@src/index.css
@components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and apply to layout and controls</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, src/components/ui/separator.tsx, src/components/ui/tooltip.tsx, src/index.css, src/routes/index.tsx, src/components/layout/Sidebar.tsx, src/components/layout/AppShell.tsx</files>
  <action>
    1. Run `npx shadcn@latest add button card input badge separator tooltip` to generate all needed UI components.
    2. Ensure `src/index.css` has the `dark` class activation. If the `<html>` element doesn't have `class="dark"`, add it in `index.html`. The shadcn CSS variables in index.css should already have dark mode definitions from the initial setup.
    3. In `src/routes/index.tsx`:
       - Replace the project directory `<input>` with shadcn `<Input>` (import from `@/components/ui/input`). Keep the same value/onChange/placeholder. Apply `className="flex-1 h-8 font-mono text-xs"` for sizing.
       - Replace the "Browse" `<button>` with `<Button variant="outline" size="sm">Browse</Button>`.
       - Replace the "Review Changes" `<button>` with `<Button size="sm">Review Changes</Button>`.
    4. In `src/components/layout/Sidebar.tsx`:
       - Replace the Settings `<button>` with `<Button variant="ghost" size="sm" className="w-full justify-start gap-2">`. Use `Settings` icon from lucide-react instead of inline SVG.
       - Apply `Separator` between the nav area and settings button instead of `border-t`.
    5. In `src/components/layout/AppShell.tsx`, verify the root div has appropriate dark mode classes. No major changes needed if it already uses `bg-zinc-950`.
  </action>
  <verify>
    <automated>cd /Users/birkankader/Documents/Projects/WhaleCode && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>shadcn Button, Card, Input, Badge, Separator, Tooltip components generated. index.tsx uses Input and Button. Sidebar uses Button and lucide-react icon. Dark mode active.</done>
</task>

<task type="auto">
  <name>Task 2: Apply shadcn components to WorktreeStatus, StatusPanel, and ProcessPanel</name>
  <files>src/components/WorktreeStatus.tsx, src/components/status/StatusPanel.tsx, src/components/terminal/ProcessPanel.tsx, src/components/settings/ApiKeySettings.tsx</files>
  <action>
    1. In `WorktreeStatus.tsx`:
       - Wrap the entire component in `<Card>` / `<CardHeader>` / `<CardContent>` from shadcn. Replace the current `<div className="rounded border border-zinc-800 bg-zinc-900/50">` with `<Card>`.
       - Replace the header with `<CardHeader className="py-3 px-4"><CardTitle className="text-sm">Active Worktrees</CardTitle></CardHeader>`.
       - Use `<Badge variant="secondary">` for the worktree count instead of the hand-styled span.
       - Replace the "Refresh" button with `<Button variant="ghost" size="sm">`.
       - Replace the "Cleanup Stale Worktrees" button with `<Button variant="outline" size="sm">`.
       - Replace per-worktree "Merge" buttons with `<Button variant="default" size="sm" className="bg-blue-600/20 text-blue-400 hover:bg-blue-600/30">` (or a custom variant).
    2. In `StatusPanel.tsx`:
       - Replace status indicator badges (running/completed/failed) with `<Badge>` using appropriate variants: `variant="default"` for running, `variant="secondary"` for completed, `variant="destructive"` for failed.
       - Replace any hand-styled buttons with shadcn `<Button>`.
    3. In `ProcessPanel.tsx`:
       - Replace the task input submit button with `<Button size="sm">`.
       - Replace tool override buttons with `<Button variant="outline" size="sm">` or `<Button variant="secondary" size="sm">`.
       - Keep xterm.js terminal styling untouched (OutputConsole) -- only modify the control bar area.
    4. In `ApiKeySettings.tsx`:
       - Replace the "Save" / "Delete" buttons with `<Button>` and `<Button variant="destructive">`.
       - Replace API key `<input>` fields with shadcn `<Input>`.
       - Replace tab buttons with `<Button variant="ghost">` with active state.
    5. Do NOT modify DiffReview internals, OutputConsole/xterm, or ConflictAlert -- those work fine.
  </action>
  <verify>
    <automated>cd /Users/birkankader/Documents/Projects/WhaleCode && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>WorktreeStatus uses Card/Badge. StatusPanel uses Badge for status. ProcessPanel uses Button. ApiKeySettings uses Button/Input. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete UI modernization</name>
  <action>
    Human verifies the full UI modernization visually: resizable worktree panel (from plan 02), conditional review controls (from plan 02), sidebar cleanup (from plan 02), and shadcn component integration across all high-visibility surfaces (this plan).
  </action>
  <verify>Human confirms visual quality and functional correctness</verify>
  <done>User approves the overall look and feel of the modernized UI</done>
  <what-built>Complete UI modernization: resizable worktree panel, conditional review controls, sidebar cleanup, shadcn component integration across all high-visibility surfaces</what-built>
  <how-to-verify>
    1. Run `npm run dev` (or `cargo tauri dev`) to launch the app
    2. Verify the worktree panel at the bottom has a visible drag handle -- drag to resize it
    3. Verify the worktree panel can be collapsed by dragging to minimum
    4. Set a project directory and verify the input field and Browse button look polished (shadcn styling)
    5. Check the sidebar -- "Claude Code" button should be gone, Settings button should use lucide icon
    6. Open Settings -- verify API key inputs and buttons are styled consistently
    7. Check StatusPanel badges when a task exists
    8. When no worktrees exist, verify "Cleanup Stale Worktrees" button is hidden
    9. Overall: app should look cohesive with consistent button/card/input styling
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
# TypeScript compilation
cd /Users/birkankader/Documents/Projects/WhaleCode && npx tsc --noEmit 2>&1 | head -20

# Verify shadcn components exist
ls src/components/ui/{button,card,input,badge,separator,tooltip,resizable}.tsx

# Rust tests still pass (no Rust changes in this plan)
cd src-tauri && cargo test --lib 2>&1 | tail -5
```
</verification>

<success_criteria>
- All shadcn UI components (button, card, input, badge, separator, tooltip) generated and used
- Dark mode CSS variables active
- WorktreeStatus wrapped in Card with Badge for count
- StatusPanel uses Badge for status indicators
- ProcessPanel controls use Button components
- ApiKeySettings uses Input and Button
- No regression in existing functionality
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish-bug-fixes/10-03-SUMMARY.md`
</output>
